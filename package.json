{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "client": "npm run start --prefix app",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "prettier": "prettier --write './app/src/**/*.{scss,js,md}' './{middlewares,models,routes}/**/*.js'",
    "heroku-postbuild": "cd app && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.1",
    "dotenv": "^8.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/core": "^7.8.6",
    "@babel/node": "^7.8.4",
    "prettier": "^1.19.1"
  },
  "engines": {
    "node": "10.12.0"
  }
}
